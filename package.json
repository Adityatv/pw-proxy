{
  "name": "pw-site-proxy",
  "version": "1.0.0",
  "description": "Reverse proxy that serves pwthor.site and replaces header image with custom image",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "http-proxy-middleware": "^2.0.6",
    "cheerio": "^1.0.0-rc.12",
    "node-fetch": "^2.6.7"
  },
  "engines": {
    "node": ">=16"
  }
}